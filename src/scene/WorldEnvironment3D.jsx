import React from 'react';
import {Float,Text} from '@react-three/drei';
export default function WorldEnvironment3D({environment,onActivate}){if(!environment)return null;const y=-.9;return <group position={[environment.x||0,environment.y||y,environment.z||0]} onClick={e=>{e.stopPropagation();onActivate?.(environment)}}><Float speed={1.25} floatIntensity={.12}><mesh><boxGeometry args={[.72,.24,.52]}/><meshStandardMaterial color={environment.active?'#f6d365':'#6b7280'} emissive={environment.active?'#6b4f00':'#202020'} emissiveIntensity={environment.active?.45:.1}/></mesh><Text position={[0,.45,0]} fontSize={.14} color="white" anchorX="center">{environment.label}</Text><Text position={[0,.26,.28]} fontSize={.18} anchorX="center">{environment.kind==='celebrate'?'🎉':environment.kind==='beacon'?'⭐':environment.kind==='garage'?'🔧':environment.kind==='checkpoint'||environment.kind==='finish'?'🏁':environment.kind==='station'?'🚉':environment.kind==='signal'?'🚦':environment.kind==='cargo'?'📦':environment.kind==='egg'?'🥚':environment.kind==='search'?'🔎':'🚀'}</Text></Float></group>}
