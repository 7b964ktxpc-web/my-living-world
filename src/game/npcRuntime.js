const NPC_SPAWNS={space:[{id:'bot-pilot',x:-2.7,z:-1.1,behavior:'patrol',radius:1.8}],cars:[{id:'mechanic',x:-2.7,z:-.8,behavior:'wander',radius:1.4}],trains:[{id:'conductor',x:-2.8,z:.8,behavior:'stationary',radius:.6}],dinos:[{id:'ranger',x:-2.4,z:-1.0,behavior:'wander',radius:1.6}]};
export function npcSpawnsForWorld(world){return NPC_SPAWNS[world]||[]}
export function createNpcState(world,seed=1){return npcSpawnsForWorld(world).map((npc,index)=>({...npc,world,homeX:npc.x,homeZ:npc.z,phase:(seed+index)*.8,heading:0,state:'idle',followTarget:null}))}
export function tickNpc(npc,delta,now=0){if(!npc)return npc;const phase=npc.phase+(delta||0);if(npc.state==='following'||npc.state==='returning')return {...npc,phase,updatedAt:now};if(npc.behavior==='stationary')return {...npc,phase,state:npc.state==='celebrate'||npc.state==='greeting'?npc.state:'idle'};const x=npc.x+Math.cos(phase*.7)*npc.radius*.025;const z=npc.z+Math.sin(phase*.9)*npc.radius*.025;return {...npc,phase,x,z,heading:Math.atan2(Math.cos(phase*.7),-Math.sin(phase*.9)),state:npc.state==='celebrate'||npc.state==='greeting'||npc.state==='pointing'?npc.state:'walking',updatedAt:now}}
export function npcReaction(npc,event){if(!npc)return null;if(event==='near')return {type:'npc_reaction',npcId:npc.id,state:'greeting'};if(event==='success')return {type:'npc_reaction',npcId:npc.id,state:'celebrate'};if(event==='objective')return {type:'npc_reaction',npcId:npc.id,state:'pointing'};return null}
