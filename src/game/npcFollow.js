const OFFSETS={space:{x:-.65,z:.45},cars:{x:-.8,z:.5},trains:{x:-.7,z:.65},dinos:{x:-.6,z:-.55}};
export function npcFollowTarget(npc,target,world){if(!npc||!target)return npc;const offset=OFFSETS[world]||OFFSETS.space;const dx=(target.x+offset.x)-npc.x,dz=(target.z+offset.z)-npc.z;const d=Math.hypot(dx,dz);if(d<.06)return {...npc,state:'following'};const step=Math.min(d,.9*.016*60);return {...npc,x:npc.x+(dx/d)*step,z:npc.z+(dz/d)*step,heading:Math.atan2(dx,dz),state:'following'} }
export function npcReturnHome(npc,world){if(!npc||npc.homeX==null)return npc;const dx=npc.homeX-npc.x,dz=npc.homeZ-npc.z,d=Math.hypot(dx,dz);if(d<.08)return {...npc,x:npc.homeX,z:npc.homeZ,state:'idle'};const step=Math.min(d,.75*.016*60);return {...npc,x:npc.x+(dx/d)*step,z:npc.z+(dz/d)*step,heading:Math.atan2(dx,dz),state:'returning'} }
